# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wandering_inn_tracker
DB_USER=wandering_inn
DB_PASSWORD=dev_password_change_in_production

# Scraper Configuration
# Base URL for The Wandering Inn chapters
BASE_URL=https://wanderinginn.com

# Rate limiting (seconds between requests)
# Default: 10 (respects robots.txt crawl-delay)
REQUEST_DELAY=10

# Starting chapter (for incremental scraping)
START_CHAPTER=1

# Maximum chapters to scrape in one run (0 = unlimited)
MAX_CHAPTERS=0
